
SubsCopy
normal publish, subscriber just past a pointer
from sender (publisher) to all receivers.

SubsCopy is an example of subscriber which get the pointer
copies to an own object and pass just the copy to the receiver.

------------------

Filter:
receiver using a lowpassfilter from the signalprocessing support



